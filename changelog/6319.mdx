- [#6319](https://github.com/RasaHQ/rasa/issues/6319): Add Prometheus `/metrics` endpoint support

To enable the prometheus integrations, add the following block to your `endpoints.yml`:

```yml
prometheus:
  enable: true
```
